- Chat app with responsive UI for mobile devices
- Server is built in Node.js with Express.js for routing. User authentication is built using Passport.js. Data layer is built using Bookshelf.js. Used MySQL for the database.
- Used Bootstrap and EJS templating to build the responsive front end for mobile clients
- Features include adding friends, individual and group chats, and formatted code snippets